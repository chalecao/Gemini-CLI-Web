# Backend server port
PORT=4008

# Frontend port (Vite)
VITE_PORT=4009

# CLI configuration
# CLI_NAME: gemini or codebuddy
CLI_NAME=gemini

# CLI executable path (optional)
# If the CLI is not in your PATH, specify the full path here
# Example: CLI_PATH=/home/username/.nvm/versions/node/v22.17.0/bin/gemini
# CLI_PATH=gemini

# Database file path (optional)
# DATABASE_PATH=./data/database.sqlite

# JWT Secret (required for production)
# Generate a secure random string for production use
# openssl rand -base64 32 (run this in your terminal)
JWT_SECRET=your-secret-key-here

# Node environment
NODE_ENV=development